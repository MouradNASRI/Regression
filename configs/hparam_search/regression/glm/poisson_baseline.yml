name: "poisson_baseline"
model: "poisson"
metric: "mse"
mode: "min"
strategy: "grid"

base_model_params:
  max_iter: 1000
  tol: 0.0001
  warm_start: false
  verbose: 0

params:
  alpha: [0.0, 0.1, 1.0]      # 0 = unregularized, 0.1 & 1.0 add L2
  fit_intercept: [true, false]
