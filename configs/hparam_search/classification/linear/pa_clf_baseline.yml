name: "pa_clf_baseline"
model: "pa_clf"
metric: "accuracy"
mode: "max"
strategy: "grid"

base_model_params:
  max_iter: 1000
  shuffle: true
  random_state: 42

params:
  C: [0.01, 0.1, 1.0, 10.0]
  loss: ["hinge", "squared_hinge"]
  fit_intercept: [true, false]
  average: [false, true]
